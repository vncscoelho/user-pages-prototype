/* eslint-disable no-unused-vars */
import { shallowMount } from '@vue/test-utils';
import BaseCheckbox from '@/components/UI/BaseCheckbox.vue';

describe('BaseCheckbox.vue', () => {
  it('will render labels when prop label is given', () => {});
  it('will add its own value to the array when checked', () => {});
  it('will remove its own value from array when unchecked', () => {});
});
