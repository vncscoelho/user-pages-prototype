/* eslint-disable no-unused-vars */
import { shallowMount } from '@vue/test-utils';
import BaseInput from '@/components/UI/BaseInput.vue';

describe('BaseInput.vue', () => {
  it('will trigger a input event when value is changed', () => {});
});
